// const guildMemberAdd = require("../events/guild/guildMemberAdd");

// module.exports = {
//     name: 'update',
//     description: "Adds new ranks",
//     execute(client, message, args, Discord, profileData){
//         ('guildMemberAdd', guildMember =>{const profile = profileData.points
//         let role1 = guildMember.guild.roles.cache.find(role => role.name === 'Menig');
//         if (profile == 2 && !(profile > 4)) guildMember.roles.add(role1);
        
//         })
//     }
// }